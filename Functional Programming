# Functional Programming
## What is functional programming?
"Treats computation as the evaluation of mathematical functions and avoids changing state and mutable data"
## What is a pure function and how do we know if something is a pure function?
Same input always gives same outpout. Check if it doesn't modify any external state and no side effects.
## What are the benefits of a pure function?
Predicable, testable, reusable, 
## What is immutability?
Prevents its state from being changed
## What is Referential transparency?
Expression can be replaced with its value without changing behavior
# Node JS Tutorial for Beginners #6 - Modules and require()
## What is a module?
Reusable code
## What does the word ‘require’ do?
Import modules
## How do we bring another module into the file the we are working in?
const moduleName = require('module-name');
## What do we have to do to make a module available?
module.exports